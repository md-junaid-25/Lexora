import React from 'react';

const Header = ({ toggleSidebar,activePage, onNavigate }) => {
  return (
    <header>
      <h1 className="app-title">Lexora</h1>
    </header>
  );
};

export default Header;